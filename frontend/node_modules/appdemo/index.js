//创建服务器实例(类似创建tomcat)
var express = require('express');
var app = express();
//创建消息体解析实例
//var bodyParser = require('body-parser');
//app.use(bodyParser.json());

//引入路由(类似引入servlet)
app.use('/user',require('./core/users.js'));

app.listen(8080);  //启用(类似启动tomcat)